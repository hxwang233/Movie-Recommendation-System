export interface Options {
    field: string;
    size?: [number, number];
    round?: boolean;
    ratio?: number;
    padding?: number;
    sort?: boolean;
    as?: [string, string];
}
export declare function partition(data: any, options: Options): any[];
