import { Data, Meta } from '../types';
import { NodeLinkData } from '../types/relation-data';
/**
 * 查看数据是否是全负数、或者全正数
 * @param data
 * @param field
 */
export declare function adjustYMetaByZero(data: Data, field: string): Meta;
/**
 * 转换数据格式为带有节点与边的数据格式
 * @param data
 * @param sourceField
 * @param targetField
 * @param weightField
 */
export declare function transformDataToNodeLinkData(data: Data, sourceField: string, targetField: string, weightField: string): NodeLinkData;
