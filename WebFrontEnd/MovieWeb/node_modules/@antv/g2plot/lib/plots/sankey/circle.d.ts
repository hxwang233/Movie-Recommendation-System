import { Data } from '../../types';
/**
 * 切断桑基图数据中的环（会丢失数据），保证顺序
 * @param data
 * @param sourceField
 * @param targetField
 */
export declare function cutoffCircle(data: Data, sourceField: string, targetField: string): Data;
