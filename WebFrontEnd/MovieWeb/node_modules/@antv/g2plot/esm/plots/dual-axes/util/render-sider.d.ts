import { View } from '@antv/g2';
/**
 * 生成右侧 View 的 slider
 * 由于双轴图是多 View , 需要监听左侧 Slider 的 change 事件来同步右侧 View
 * @param { View } view 右侧视图
 * @param { number[] } sliderValue 滑块当前值
 * @returns void
 */
export declare const renderSlider: (view: View, sliderValue: [number, number]) => void;
