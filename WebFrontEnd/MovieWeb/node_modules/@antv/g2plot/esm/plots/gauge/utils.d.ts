import { Data } from '../../types';
import { GaugeOptions } from './types';
/**
 * 将 range 生成为 data 数据
 * @param range
 * @param key
 */
export declare function processRangeData(range: number[]): Data;
/**
 * 获取 仪表盘 指针数据
 * @param percent
 */
export declare function getIndicatorData(percent: GaugeOptions['percent']): Data;
/**
 * 获取仪表盘 表盘弧形数据
 * @param percent
 * @param range
 */
export declare function getRangeData(percent: GaugeOptions['percent'], range?: GaugeOptions['range']): Data;
